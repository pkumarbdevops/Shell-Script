#!/bin/bash

# this is the comment, we need to develop a programm that can do addition of 2 number

NUMBER=$1 # passing the values from the command line
NUMBER=$2 # passing the values from the command line
# I need to runa command inside shell to add these 2 number
SUM=$(NUMBER1+NUMBER2)

echo "addition of 2 number is: $SUM"